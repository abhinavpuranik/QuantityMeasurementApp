# QuantityMeasurementApp

A browser-based unit converter built with vanilla JavaScript and a json-server backend. Supports three modes — Conversion (convert a value between units), Comparison (compare two measurements), and Arithmetic (add, subtract, multiply, or divide across units) — across four categories: Length, Weight, Volume, and Temperature. All calculations are saved to a persistent history log via a REST API backed by db.json
